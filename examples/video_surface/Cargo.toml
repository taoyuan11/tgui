[package]
name = "video_surface"
version = "0.1.0"
edition = "2021"

[dependencies]
tgui = { path = "../..", features = ["video-static"] }

[profile.release]
lto = true
codegen-units = 1
panic = 'abort'
debug = false
opt-level = 3