[package]
name = "demo"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
tgui = { workspace = true, features = [
    "audio",
    "video",
] }

[features]
collect-profile = ["tgui/collect-profile"]
