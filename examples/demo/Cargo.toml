[package]
name = "demo"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
tgui = { path = "../..", features = [
    "audio",
    "video",
] }

[features]
collect-profile = ["tgui/collect-profile"]
